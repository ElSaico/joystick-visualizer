{
  "name": "joystick-visualizer",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "preact watch",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "lint": "eslint srcy"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "preact-cli": "^3.0.0",
    "sirv-cli": "1.0.3"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  }
}